class MKWii {
    constructor(){
        this.param = 'param is not a method hehe'
    }
    getAll(text) {
        return 'getAll should return this string with the text ' + text;
    }
}

module.exports = MKWii